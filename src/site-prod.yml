$include:
  - site.yml
  - site-i18n.yml
path: /staging.iris.github.io/
static-path: static
url: https://arrol.github.io/pages_smple/docs
# we generate links without extensions as nginx rewrites those URLs automatically
hide-href-extensions: ['/index.html', 'index.html', '.html']
context:
  env: production
